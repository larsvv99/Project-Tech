<h1>testing</h1>

{{#each keuken}}
<input type="checkbox" name="keuken" value="{{keuken}}" id="{{keuken}}">
<label for="{{keuken}}">
    <img src="images/{{keuken}}.jpg">
    <h2>{{keuken}}</h2>
</label>
{{/each}}